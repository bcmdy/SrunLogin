<Window x:Class="SrunLogin.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="校园网认证工具"
        Width="920"
        Height="720"
        MinWidth="820"
        MinHeight="620"
        Background="{StaticResource AppBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="校园网认证工具" FontSize="24" FontWeight="Bold" />
                <TextBlock Text="SRun / Dr.COM 登录、注销与在线状态查询" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
            </StackPanel>
            <TextBlock DockPanel.Dock="Right"
                       Text="{Binding LoopStatus}"
                       Foreground="{StaticResource MutedBrush}"
                       HorizontalAlignment="Right" />
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="480" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="1.05*" MinWidth="280" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="网关地址" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="用户名" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="密码" />
                        <PasswordBox x:Name="PasswordInput" Grid.Row="4" Grid.Column="1" PasswordChanged="PasswordInput_PasswordChanged" />

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="IP 地址" />
                        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="8" Grid.Column="0" Text="AC ID" />
                        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding AcId, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="10" Grid.Column="0" Text="域" />
                        <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding Domain, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,18,0,16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <CheckBox Content="保存配置" IsChecked="{Binding SaveConfig}" VerticalAlignment="Center" />
                            <CheckBox Content="自动登录" IsChecked="{Binding AutoLogin}" VerticalAlignment="Center" Margin="22,0,0,0" />
                        </StackPanel>

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Content="登录" Command="{Binding LoginCommand}" Background="{StaticResource PrimaryBrush}" MinWidth="96" />
                            <Button Grid.Column="2" Content="查询" Command="{Binding InfoCommand}" Background="{StaticResource InfoBrush}" MinWidth="96" />
                            <Button Grid.Column="4" Content="注销" Command="{Binding LogoutCommand}" Background="{StaticResource DangerBrush}" MinWidth="96" />
                            <Button Grid.Column="6" Content="帮助" Command="{Binding HelpCommand}" Background="{StaticResource WarningBrush}" Foreground="{StaticResource TextBrush}" MinWidth="96" />
                        </Grid>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="输出日志" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBox Grid.Row="1"
                                 Text="{Binding Output}"
                                 Height="Auto"
                                 MinHeight="220"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 HorizontalContentAlignment="Left"
                                 VerticalContentAlignment="Top"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"
                                 FontSize="13"
                                 TextWrapping="NoWrap" />
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Padding="18">
                <StackPanel>
                    <TextBlock Text="高级功能" FontSize="18" FontWeight="Bold" Margin="0,0,0,14" />
                    <CheckBox Content="启用循环检测" IsChecked="{Binding LoopEnabled}" Margin="0,0,0,16" />

                    <TextBlock Text="检测间隔（秒）" Foreground="{StaticResource MutedBrush}" />
                    <TextBox Text="{Binding LoopInterval, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,14" />

                    <TextBlock Text="网络超时（秒）" Foreground="{StaticResource MutedBrush}" />
                    <TextBox Text="{Binding LoopTimeout, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,14" />

                    <TextBlock Text="Ping 主机" Foreground="{StaticResource MutedBrush}" />
                    <TextBox Text="{Binding PingHost, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,20" />

                    <TextBlock Text="循环检测会在网络不可达时尝试重新登录。" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
